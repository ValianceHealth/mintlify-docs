---
title: "Models"
description: "The full model catalog the gateway serves, grouped by provider, with the processor and assessment status of every model."
---

The gateway serves models from five providers. Every model runs under a signed BAA — AWS or Microsoft. Whatever model you pick, the redaction path is identical: identifiers never reach the model.

**Where each model processes your data is covered by our PDPA Transfer Impact Assessment**, which names every destination. We share it under NDA — ask us, and ask before you build against a model if residency is a requirement for you.

**The assessment is ours to complete, not yours to negotiate.** A model whose destination we have assessed is callable by every customer. One we have not assessed is callable by none, and returns `403` with `code: "model_not_allowed"` until that changes. The **Transfer assessment** column below tells you which is which today.

If you omit `model`, the gateway uses `apac.amazon.nova-pro-v1:0`.

**Read the model ID as a destination, not just a capability.** A prefix names where inference happens: `apac.` is the ten Asia-Pacific AWS regions, `us.` is the four commercial US regions, `in.` is India's two, and `jp.` is Japan's two. `global.` names no single jurisdiction and therefore cannot carry a completed assessment, whatever its BAA status.

A model ID with **no** prefix is the narrowest case of all: it is served from one region only, with no cross-region routing. That is why the Japan group below is the shortest chain we offer.

A signed BAA covers the *service*; a transfer assessment needs the *country*. Both are required, and they are not the same question.

### Reachable from Malaysia, processed elsewhere

These are two different questions and the difference matters, so we separate them plainly.

| | Answer |
| --- | --- |
| Can you **call** the gateway from Malaysia? | **Yes.** The gateway itself runs in `ap-southeast-5` (AWS Malaysia), and every model in this catalog is callable from there — and from anywhere else you reach the API |
| Is your prompt **processed** in Malaysia? | **No.** Inference happens in the destination named by the model ID — Japan, India, the US, or an Asia-Pacific region set |

Nothing here limits where you can *use* the gateway. What we cannot offer today is **inference inside Malaysia**: AWS publishes no in-region or Asia-Pacific-scoped route for the frontier model families in `ap-southeast-5`, and the Malaysia region's own catalog carries only a small set of models. Where a family is reachable from `ap-southeast-5`, it is reachable via worldwide routing, which names no destination and therefore cannot carry a completed assessment.

The same holds on the Azure side: our Azure AI Foundry deployment is a US resource, not a Malaysian one.

<Note>
  If in-country processing is a hard requirement in your contract, tell us before you build. It changes which models are available to you, and for some families the honest answer today is none of them. It is a solvable problem for specific models, and not a general one.
</Note>

Every model in this catalog supports [tool calling](/api/tool-calling). Models without tool support are not listed.

## Amazon

| Model ID | Tier | Processor | Transfer assessment |
| --- | --- | --- | --- |
| `apac.amazon.nova-pro-v1:0` | Standard (default) | AWS Bedrock — BAA, HIPAA-aligned | Complete |
| `apac.amazon.nova-lite-v1:0` | Light | AWS Bedrock — BAA, HIPAA-aligned | Complete |
| `apac.amazon.nova-micro-v1:0` | Micro | AWS Bedrock — BAA, HIPAA-aligned | Complete |

The default family, and the one with a completed assessment. Pick these unless you have a reason not to.

## Anthropic

| Model ID | Tier | Processor | Transfer assessment |
| --- | --- | --- | --- |
| `global.anthropic.claude-opus-5` | Frontier (most capable) | AWS Bedrock — BAA, HIPAA-aligned | Complete |
| `global.anthropic.claude-sonnet-5` | Frontier | AWS Bedrock — BAA, HIPAA-aligned | Complete |
| `global.anthropic.claude-haiku-4-5-20251001-v1:0` | Fast | AWS Bedrock — BAA, HIPAA-aligned | Complete |

Same AWS account and BAA as the Nova family.

## OpenAI

| Model ID | Tier | Processor | Transfer assessment |
| --- | --- | --- | --- |
| `us.openai.gpt-5.6-terra` | Frontier | AWS Bedrock — BAA, HIPAA-aligned | Complete — United States |
| `us.openai.gpt-5.6-luna` | Frontier | AWS Bedrock — BAA, HIPAA-aligned | Complete — United States |
| `us.openai.gpt-5.6-sol` | Frontier | AWS Bedrock — BAA, HIPAA-aligned | Complete — United States |
| `in.openai.gpt-5.6-terra` | Frontier | AWS Bedrock — BAA, HIPAA-aligned | Complete — India |
| `in.openai.gpt-5.6-luna` | Frontier | AWS Bedrock — BAA, HIPAA-aligned | Complete — India |
| `gpt-5.5` | Frontier | Azure AI Foundry — Microsoft BAA | Open |
| `gpt-5.6-luna` | Frontier | Azure AI Foundry — Microsoft BAA | Open |
| `gpt-5.6-sol` | Frontier | Azure AI Foundry — Microsoft BAA | Open |
| `openai.gpt-oss-120b-1:0` | Open-weight | AWS Bedrock — BAA, HIPAA-aligned | Open |
| `openai.gpt-oss-20b-1:0` | Open-weight, fast | AWS Bedrock — BAA, HIPAA-aligned | Open |

**The `us.` and `in.` prefixes are the destination.** These are the same GPT-5.6 models, served from a single named jurisdiction — the United States, or India — rather than from a pool. That is what makes their assessment completable, and it is why the prefix is part of the model ID rather than a separate parameter: naming the model names the destination.

Pick `in.` for the shortest chain — one country, one assessment. Pick `us.` for the widest capacity. The Azure-served `gpt-5.x` rows are a different route to the same family and remain unassessed.

<Warning>
  **These five models retain flagged traffic for up to 30 days.** AWS applies zero data retention to Bedrock by default, but the GPT-5.6 family is an exception: traffic flagged by its abuse classifier is held up to 30 days for automated review, in the region that processed the request. It is not shared with OpenAI. If that does not suit your use case, pick a Nova or `gpt-oss` model — neither carries the exception.
</Warning>

Dialect note: the Azure-served GPT-5.x route speaks the OpenAI Responses format; the AWS-served `us.` / `in.` route speaks Bedrock Converse. The gateway routes by model ID either way, so **your request format does not change**.

## DeepSeek

| Model ID | Tier | Processor | Transfer assessment |
| --- | --- | --- | --- |
| `deepseek-v4-pro` | Frontier reasoning | Azure AI Foundry — Microsoft BAA | Open |
| `deepseek-v4-flash` | Fast | Azure AI Foundry — Microsoft BAA | Open |
| `deepseek.v3.2` | Reasoning | AWS Bedrock — BAA, HIPAA-aligned | Open |

## Served from Japan

One country, one region, and the shortest transfer chain we offer. These run on bare model IDs in `ap-northeast-1` — no cross-region routing at all, so the destination is a single place rather than a list of them.

| Model ID | Tier | Processor | Transfer assessment |
| --- | --- | --- | --- |
| `openai.gpt-oss-safeguard-120b` | Safety classifier | AWS Bedrock — BAA, HIPAA-aligned | Complete — Japan |
| `openai.gpt-oss-safeguard-20b` | Safety classifier, fast | AWS Bedrock — BAA, HIPAA-aligned | Complete — Japan |
| `openai.gpt-oss-120b-1:0` | Open-weight | AWS Bedrock — BAA, HIPAA-aligned | Complete — Japan |
| `openai.gpt-oss-20b-1:0` | Open-weight, fast | AWS Bedrock — BAA, HIPAA-aligned | Complete — Japan |
| `qwen.qwen3-235b-a22b-2507-v1:0` | Reasoning | AWS Bedrock — BAA, HIPAA-aligned | Complete — Japan |
| `zai.glm-5` | Frontier | AWS Bedrock — BAA, HIPAA-aligned | Complete — Japan |
| `zai.glm-4.7` | Balanced | AWS Bedrock — BAA, HIPAA-aligned | Complete — Japan |
| `moonshotai.kimi-k2.5` | Reasoning | AWS Bedrock — BAA, HIPAA-aligned | Complete — Japan |
| `mistral.mistral-large-3-675b-instruct` | Frontier | AWS Bedrock — BAA, HIPAA-aligned | Complete — Japan |

**Why this group is the shortest chain.** Three things line up here that rarely line up together: the destination is a single country, AWS applies zero data retention by default and none of these models carries a retention exception, and the weights run in AWS-operated accounts so the model's authors receive nothing. If residency is the deciding factor for you, start here.

<Note>
  These models are **processed in Japan**, which is still a cross-border transfer from Malaysia. "Single country" means the assessment names one jurisdiction instead of many — not that the data stays in Malaysia. You can call them from Malaysia; the inference happens in Tokyo. See [Reachable from Malaysia, processed elsewhere](#reachable-from-malaysia-processed-elsewhere) above.
</Note>

## MiniMax

| Model ID | Tier | Processor | Transfer assessment |
| --- | --- | --- | --- |
| `minimax.minimax-m2.5` | Reasoning | AWS Bedrock — BAA, HIPAA-aligned | Open |

Note: this model emits reasoning tokens billed as output, so its cost per task runs above its per-token rate card.

## Choosing a model

```bash
curl https://api.v2.healthproximate.com/api/v1/llm/chat/completions \
  -H "Authorization: Bearer hpx_..." \
  -H "Content-Type: application/json" \
  -d '{
    "model": "apac.amazon.nova-pro-v1:0",
    "messages": [{"role": "user", "content": "Summarize this discharge note: ..."}]
  }'
```

<Info>
  **The catalog is the same for every customer.** A model is callable once its destination is covered by our transfer assessment, and refused until then — the same set for everyone, not a per-account menu. Models not yet covered return `403` with `code: "model_not_allowed"`.

  There is no region or routing parameter on the request. The model you name determines how it is served, so the model id is the whole choice — not a per-call flag we would have to trust the caller to set.
</Info>

## Errors you may see

| Status | Code | Meaning |
| --- | --- | --- |
| `403` | `model_not_allowed` | This model's destination is not yet covered by our transfer assessment, so it is not callable by anyone. Pick a model whose **Transfer assessment** reads Complete. |
| `503` | — | The redaction engine is unavailable. The gateway fails closed rather than forward un-redacted text. |

See [Errors & limits](/api/errors-and-limits) for the full error contract.
