openapi: 3.0.3
info:
  title: Healthproximate API
  version: 1.0.0
  description: 'DRG classification, OMOP vocabulary services and a PHI-safe LLM gateway.


    Generated by `manage.py export_commercial_openapi` from an explicit operation allowlist (`app/openapi_commercial.py`).
    An endpoint appears here because someone published it, never because it exists.'
  contact:
    email: admin@valiancehealth.ai
paths:
  /api/v1/drg/agent/:
    post:
      operationId: v1_drg_agent_create
      description: "\nUse an AI agent to classify hospital encounters into MS-DRG codes from\nnatural\
        \ language clinical text. The agent can extract ICD-10 codes from\nclinical descriptions and explain\
        \ DRG assignments.\n\n**LLM cost:** ~$0.01-0.05 per request (uses Claude Sonnet via AWS Bedrock).\n\
        \nFor structured ICD-10 code input, use the `/api/v1/drg/classify/` endpoint instead ($0 cost).\n\
        \    "
      summary: AI agent for DRG classification from clinical text
      tags:
      - DRG
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DRGAgentRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/DRGAgentRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/DRGAgentRequestRequest'
        required: true
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DRGAgentResponse'
              examples:
                HIVDiagnosis:
                  value:
                    response: Based on the diagnosis code B20 (HIV disease), this classifies to **DRG
                      977** - HIV WITH RELATED CONDITION...
                    token_usage:
                      input_tokens: 1520
                      output_tokens: 340
                    response_time_ms: 3200
                  summary: HIV Diagnosis
          description: Agent response with DRG classification
        '400':
          description: Invalid request
        '401':
          description: Authentication required
        '500':
          description: Internal server error
  /api/v1/drg/batch/:
    post:
      operationId: v1_drg_batch_create
      description: "\nSubmit up to 1000 cases in one call; classification runs asynchronously.\nReturns\
        \ a job id immediately — poll `GET /api/v1/drg/batch/{job_id}/` for\nstatus and progressive per-case\
        \ results.\n\n**Idempotency-Key header is required.** Retrying a submission with the same\nkey\
        \ returns the same job (HTTP 200 instead of 201) and never re-processes or\nre-bills — safe to\
        \ retry on any network failure.\n\nEach case: `case_ref` (your identifier, unique within the batch),\
        \ `dx_codes`\n(ICD-10-CM, primary first), optional `pr_codes` (ICD-10-PCS), `gender`,\n`is_alive`.\
        \ `source_dialect` currently accepts `icd10cm` only — WHO ICD-10 /\nICD-11 batches are refused\
        \ up front rather than mis-grouped.\n\nBilling: one ledger row per case (`service_type=\"drg_batch\"\
        `), written exactly\nonce even across worker retries. A spend-capped organization's submission\
        \ is\nrefused at enqueue with a 429 and costs nothing.\n    "
      summary: Submit a batch of coded cases for DRG classification
      parameters:
      - in: header
        name: Idempotency-Key
        schema:
          type: string
        description: Caller-chosen key (8-128 chars) making the submission retry-safe.
        required: true
      tags:
      - DRG
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DRGBatchRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/DRGBatchRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/DRGBatchRequestRequest'
        required: true
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '201':
          description: Job created
        '200':
          description: Idempotent replay — existing job returned
        '400':
          description: Invalid request, unsupported dialect, or missing Idempotency-Key
        '401':
          description: Authentication required
        '429':
          description: Batch rate limit, monthly limit, or spend cap reached
  /api/v1/drg/batch/{job_id}/:
    get:
      operationId: v1_drg_batch_retrieve
      description: "\nStatus of a batch job with per-case results as they complete. Poll this —\na job\
        \ in `queued`/`running` will keep gaining `done`/`failed` cases; terminal\njob states are `done`\
        \ (at least one case succeeded), `failed` (every case\nfailed), and `parked` (the organization\
        \ hit its spend limit; the job resumes\nautomatically when the limit clears).\n\nCase results\
        \ carry your `case_ref`, so you can join them back to your\nrecords. Failed cases carry an `error`\
        \ string; they were not billed as\nsuccessful classifications.\n    "
      summary: Batch job status and progressive results
      parameters:
      - in: path
        name: job_id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - DRG
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          description: Job status
        '401':
          description: Authentication required
        '404':
          description: No such job in your organization
  /api/v1/drg/classify/:
    post:
      operationId: v1_drg_classify_create
      description: "\nClassify ICD-10-CM diagnosis codes and ICD-10-PCS procedure codes into an MS-DRG\
        \ code\nusing the vhdrg classification engine.\n\n**No LLM cost** — this endpoint uses deterministic\
        \ rule-based classification.\n\nSupported MS-DRG versions: v36 (FY2019) through v40 (FY2023),\
        \ and v43 (FY2026).\nCMS grouper data for v41/v42 is not shipped by the vhdrg engine.\n    "
      summary: Classify ICD-10 codes into MS-DRG
      tags:
      - DRG
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DRGClassifyRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/DRGClassifyRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/DRGClassifyRequestRequest'
        required: true
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DRGClassifyResponse'
              examples:
                SimplePneumonia:
                  value:
                    primary_drg: '195'
                    description: SIMPLE PNEUMONIA AND PLEURISY WITHOUT CC/MCC
                    mdc: '04'
                    type: M
                    all_candidates:
                    - '195'
                    - '000'
                    version: v40
                    fiscal_year: FY 2023
                    input:
                      dx_codes:
                      - J189
                      - J310
                      gender: F
                      is_alive: true
                      pr_codes: []
                    response_time_ms: 12
                  summary: Simple Pneumonia
          description: DRG classification result
        '400':
          description: Invalid request
        '401':
          description: Authentication required
        '500':
          description: Internal server error
  /api/v1/fhir/drg/$classify:
    post:
      operationId: v1_fhir_drg_classify_create
      description: "\nFHIR front door for the deterministic DRG classifier. Send a Bundle containing\n\
        the encounter's `Condition` resources (ICD-10-CM codings; principal marked via\n`Encounter.diagnosis`\
        \ rank 1, else first-listed), optional `Procedure`\nresources (ICD-10-PCS codings), a minimal\
        \ `Patient` (only `gender` is read),\nand optionally an `Encounter` (`hospitalization.dischargeDisposition`\
        \ maps to\ndischarge status).\n\n**Patient identifiers are dropped at the boundary** — names,\
        \ identifiers,\naddresses and telecom are never read and never stored; the response attests\n\
        this with `fhir_identifiers_dropped`. The submitted Bundle is not persisted.\n\nReturns a FHIR\
        \ `Parameters` resource carrying the DRG code, description and\ngrouper version. Malformed or\
        \ unclassifiable input returns an\n`OperationOutcome` — never a partial result.\n    "
      summary: Classify a FHIR R4 Bundle into an MS-DRG ($classify)
      tags:
      - DRG
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          description: FHIR Parameters with the DRG result
        '400':
          description: OperationOutcome — invalid or unclassifiable Bundle
        '401':
          description: Authentication required
        '429':
          description: Rate or monthly limit reached
  /api/v1/llm/chat/completions:
    post:
      operationId: v1_llm_chat_completions_create
      description: "\nAn OpenAI-compatible chat completion endpoint that redacts PHI before the\nmodel\
        \ sees it and restores the real values in the answer.\n\n```python\nclient = OpenAI(base_url=\"\
        https://<host>/api/v1/llm\", api_key=\"hpx_...\")\nclient.chat.completions.create(model=\"<model>\"\
        , messages=[...])\n```\n\nPer request: your message content is redacted (Malaysian IC/MRN/passport/phone,\n\
        names, emails, account numbers — clinical content such as ages, dates like\n\"day 3 post-op\"\
        , vitals and dosages is preserved), the placeholders go to the\nmodel, and the model's answer\
        \ has your values substituted back. The mapping\nlives only in the request's memory and is never\
        \ stored.\n\nEvery response carries `redaction_receipt`, including `rehydration_complete`\nand\
        \ `unresolved_placeholder_count` — if the model altered our placeholders, the\nreceipt says so\
        \ rather than returning residue silently.\n\n**Tool calling** is supported: send OpenAI-style\
        \ `tools`, receive `tool_calls`\nwith your values re-hydrated. Your `tool` role results are redacted\
        \ on the way\nout like any other message, and so are the arguments of an assistant turn you\n\
        send back.\n\n**Long answers: stream them.** A non-streamed request must complete inside a\nfixed\
        \ budget, and a model that has not answered by then returns `504` with\n`error.type: \"model_timeout\"\
        ` naming the model. The GPT-5.x models are the ones\nthis reaches: their latency on an identical\
        \ prompt varies widely, measured\nbetween 1.4s and 106.2s. A streamed request is not subject to\
        \ that budget at\nall, because output arrives continuously — so for long generations send\n`stream=true`\
        \ together with `redact_only=true`.\n\n**`max_tokens` minimums.** The GPT-5.x models refuse anything\
        \ below **16** and\nthe request is rejected with `400` before it reaches the provider. Other model\n\
        families have no such floor.\n\n**Not supported** (refused, not silently dropped): streaming while\
        \ re-hydrating\n(`stream` with `redact_only=false`), tools combined with `stream`, non-text\n\
        content parts, forcing a specific tool via `tool_choice`, and the deprecated\n`functions`/`function_call`\
        \ form.\n    "
      summary: PHI-safe chat completion (OpenAI-compatible)
      tags:
      - Gateway
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatCompletionRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ChatCompletionRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ChatCompletionRequestRequest'
        required: true
      security:
      - gateway_bearer_auth: []
      responses:
        '200':
          description: chat.completion with a redaction receipt attached
        '400':
          description: Invalid request, or model not approved
        '401':
          description: Authentication required
        '402':
          description: 'Insufficient prepaid credit (PAID tier only). The worst-case cost of the call
            -- its input plus max_tokens at the output rate -- is held before the model runs; when the
            wallet cannot cover that hold the call is refused here and nothing is charged. Top up and
            retry. Deliberately NOT 401 (the key is valid) and NOT 429 (nothing is rate limited): an SDK
            that retries a 429 would only hammer a wallet that needs money. `available_minor` is in the
            wallet''s own minor units.'
        '429':
          description: Rate limit or spend limit reached
        '503':
          description: 'Redaction failed — request refused. Or `error.code: capacity_exhausted` — this
            task''s gateway capacity is fully committed to in-flight requests (VAL-1000); retry shortly.'
        '504':
          description: The model did not answer inside the budget for a non-streamed request. Retry, or
            send stream=true with redact_only=true.
  /api/v1/llm/models:
    get:
      operationId: llm_list_models
      description: OpenAI-compatible model list. Returns only models THIS credential can actually call
        — a model absent here would be refused by `/api/v1/llm/chat/completions`, and a model present
        here would not.
      summary: List the models this key may call
      tags:
      - Gateway
      security:
      - gateway_bearer_auth: []
      responses:
        '200':
          description: An OpenAI-shaped model list
        '401':
          description: Missing or invalid API key
  /api/v1/omop/code/:
    post:
      operationId: v1_omop_code_create
      description: 'Resolve free text to concepts in one vocabulary.


        Retrieval routing (graph vs hybrid, and whether to narrow by OMOP domain) is chosen from the vocabulary
        capability registry, so callers do not need to know it. The `routing` block in every response
        reports what was chosen and the `evidence` behind it.


        Vocabularies whose routing has never been benchmarked are **served, not refused** — the response
        carries `measured: false` and a `caveat`.


        **Supports both JWT (Authorization: Bearer) and API key (X-API-Key) authentication.**'
      summary: Resolve text to concepts (API)
      parameters:
      - in: query
        name: response_format
        schema:
          type: string
          enum:
          - json
          - llm
          default: json
        description: Response format. 'json' (default) returns structured JSON. 'llm' returns compact
          markdown tables (text/markdown) optimized for LLM consumption. When 'llm', the response body
          is markdown text rather than the JSON schema shown below.
      tags:
      - Vocabulary
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CodeRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CodeRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CodeRequestRequest'
        required: true
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CodeResponse'
          description: Ranked concepts plus the routing decision behind them
        '400':
          description: Invalid request parameters
        '401':
          description: Invalid or missing authentication. Provide either Authorization header (Bearer
            token) or X-API-Key header.
  /api/v1/omop/resolve/:
    post:
      operationId: v1_omop_resolve_create
      description: 'Normalizes a clinical term and enriches results with ancestors, descendants, cross-vocabulary
        mappings, and concept relationships.


        Supports filtering by vocabulary, domain, and concept class. Results can be sorted by relevance
        score (default) or concept_class_id.


        **Supports both JWT (Authorization: Bearer) and API key (X-API-Key) authentication.**'
      summary: Resolve a clinical term with full ontology context (API)
      parameters:
      - in: query
        name: response_format
        schema:
          type: string
          enum:
          - json
          - llm
          default: json
        description: Response format. 'json' (default) returns structured JSON. 'llm' returns compact
          markdown tables (text/markdown) optimized for LLM consumption. When 'llm', the response body
          is markdown text rather than the JSON schema shown below.
      tags:
      - Vocabulary
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResolveRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ResolveRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ResolveRequestRequest'
        required: true
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResolveResponse'
          description: Resolved concepts with ontology context
        '400':
          description: Invalid request parameters
        '401':
          description: Invalid or missing authentication. Provide either Authorization header (Bearer
            token) or X-API-Key header.
  /api/v1/omop/resolve/ancestors/:
    post:
      operationId: v1_omop_resolve_ancestors_create
      description: 'Returns ancestor (parent) concepts in the OMOP hierarchy for a given concept.


        **Supports both JWT (Authorization: Bearer) and API key (X-API-Key) authentication.**'
      summary: Get ancestor concepts (API)
      parameters:
      - in: query
        name: response_format
        schema:
          type: string
          enum:
          - json
          - llm
          default: json
        description: Response format. 'json' (default) returns structured JSON. 'llm' returns compact
          markdown tables (text/markdown) optimized for LLM consumption. When 'llm', the response body
          is markdown text rather than the JSON schema shown below.
      tags:
      - Vocabulary
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetAncestorsRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/GetAncestorsRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/GetAncestorsRequestRequest'
        required: true
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AncestorsResponse'
          description: Ancestor concepts with separation levels
        '400':
          description: Invalid request parameters
        '401':
          description: Invalid or missing authentication. Provide either Authorization header (Bearer
            token) or X-API-Key header.
  /api/v1/omop/resolve/descendants/:
    post:
      operationId: v1_omop_resolve_descendants_create
      description: 'Returns descendant (child) concepts in the OMOP hierarchy for a given concept.


        **Supports both JWT (Authorization: Bearer) and API key (X-API-Key) authentication.**'
      summary: Get descendant concepts (API)
      parameters:
      - in: query
        name: response_format
        schema:
          type: string
          enum:
          - json
          - llm
          default: json
        description: Response format. 'json' (default) returns structured JSON. 'llm' returns compact
          markdown tables (text/markdown) optimized for LLM consumption. When 'llm', the response body
          is markdown text rather than the JSON schema shown below.
      tags:
      - Vocabulary
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetDescendantsRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/GetDescendantsRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/GetDescendantsRequestRequest'
        required: true
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DescendantsResponse'
          description: Descendant concepts with separation levels
        '400':
          description: Invalid request parameters
        '401':
          description: Invalid or missing authentication. Provide either Authorization header (Bearer
            token) or X-API-Key header.
  /api/v1/omop/resolve/expand/:
    post:
      operationId: v1_omop_resolve_expand_create
      description: 'Expands a broad clinical concept (e.g., ''eczema'') into all related OMOP concepts
        by searching for matching concepts, traversing up to ancestors, and collecting all descendants.
        Returns a flat, deduplicated concept set suitable for SQL WHERE clauses.


        Accepts one or more vocabularies via `vocabulary_ids` (e.g., `[''ICD10CM'', ''ICD10'']`). The
        expansion runs independently per vocabulary and merges into a single deduplicated set.


        **Supports both JWT (Authorization: Bearer) and API key (X-API-Key) authentication.**'
      summary: Expand a clinical concept into a full concept set (API)
      parameters:
      - in: query
        name: response_format
        schema:
          type: string
          enum:
          - json
          - llm
          default: json
        description: Response format. 'json' (default) returns structured JSON. 'llm' returns compact
          markdown tables (text/markdown) optimized for LLM consumption. When 'llm', the response body
          is markdown text rather than the JSON schema shown below.
      tags:
      - Vocabulary
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExpandRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ExpandRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ExpandRequestRequest'
        required: true
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExpandResponse'
          description: Expanded concept set with metadata
        '400':
          description: Invalid request parameters
        '401':
          description: Invalid or missing authentication. Provide either Authorization header (Bearer
            token) or X-API-Key header.
  /api/v1/omop/resolve/map/:
    post:
      operationId: v1_omop_resolve_map_create
      description: 'Maps an OMOP concept to equivalent concepts in other vocabularies.


        **Supports both JWT (Authorization: Bearer) and API key (X-API-Key) authentication.**'
      summary: Map concept to other vocabularies (API)
      parameters:
      - in: query
        name: response_format
        schema:
          type: string
          enum:
          - json
          - llm
          default: json
        description: Response format. 'json' (default) returns structured JSON. 'llm' returns compact
          markdown tables (text/markdown) optimized for LLM consumption. When 'llm', the response body
          is markdown text rather than the JSON schema shown below.
      tags:
      - Vocabulary
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MapConceptRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/MapConceptRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/MapConceptRequestRequest'
        required: true
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MapResponse'
          description: Mapped concepts from target vocabularies
        '400':
          description: Invalid request parameters
        '401':
          description: Invalid or missing authentication. Provide either Authorization header (Bearer
            token) or X-API-Key header.
  /api/v1/omop/resolve/normalize/:
    post:
      operationId: v1_omop_resolve_normalize_create
      description: 'Searches for the best-matching OMOP standard concepts for a given clinical term.


        Supports filtering by vocabulary, domain, and concept class. Results can be sorted by relevance
        score (default) or concept_class_id.


        **Supports both JWT (Authorization: Bearer) and API key (X-API-Key) authentication.**'
      summary: Normalize a clinical term to OMOP concepts (API)
      parameters:
      - in: query
        name: response_format
        schema:
          type: string
          enum:
          - json
          - llm
          default: json
        description: Response format. 'json' (default) returns structured JSON. 'llm' returns compact
          markdown tables (text/markdown) optimized for LLM consumption. When 'llm', the response body
          is markdown text rather than the JSON schema shown below.
      tags:
      - Vocabulary
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NormalizeRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/NormalizeRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/NormalizeRequestRequest'
        required: true
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NormalizeResponse'
          description: Normalized concept results with metadata
        '400':
          description: Invalid request parameters
        '401':
          description: Invalid or missing authentication. Provide either Authorization header (Bearer
            token) or X-API-Key header.
  /api/v1/omop/resolve/relationships/:
    post:
      operationId: v1_omop_resolve_relationships_create
      description: 'Returns relationships (e.g. ''Is a'', ''Subsumes'') for a given concept from the concept_relationship
        table.


        **Supports both JWT (Authorization: Bearer) and API key (X-API-Key) authentication.**'
      summary: Get concept relationships (API)
      parameters:
      - in: query
        name: response_format
        schema:
          type: string
          enum:
          - json
          - llm
          default: json
        description: Response format. 'json' (default) returns structured JSON. 'llm' returns compact
          markdown tables (text/markdown) optimized for LLM consumption. When 'llm', the response body
          is markdown text rather than the JSON schema shown below.
      tags:
      - Vocabulary
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetRelationshipsRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/GetRelationshipsRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/GetRelationshipsRequestRequest'
        required: true
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RelationshipsResponse'
          description: Related concepts with relationship types
        '400':
          description: Invalid request parameters
        '401':
          description: Invalid or missing authentication. Provide either Authorization header (Bearer
            token) or X-API-Key header.
  /api/v1/omop/resolve/workflow/:
    post:
      operationId: v1_omop_resolve_workflow_create
      description: 'Runs a multi-pass resolution workflow tailored to the vocabulary''s hierarchy. Each
        pass targets a specific concept class level (e.g., SubChapter, then Code) and optionally fetches
        descendants, giving structured step-by-step results.


        Supported vocabularies: ICD10, ICD10CM, ICD10PCS, ICD9CM, SNOMED, CPT4, HCPCS, RxNorm, LOINC.


        **Use this for hierarchical exploration.** For a flat concept set suitable for SQL filtering,
        use `/api/v1/resolve/expand/` instead.


        **Supports both JWT (Authorization: Bearer) and API key (X-API-Key) authentication.**'
      summary: Resolve a clinical term using vocabulary-specific workflow (API)
      parameters:
      - in: query
        name: response_format
        schema:
          type: string
          enum:
          - json
          - llm
          default: json
        description: Response format. 'json' (default) returns structured JSON. 'llm' returns compact
          markdown tables (text/markdown) optimized for LLM consumption. When 'llm', the response body
          is markdown text rather than the JSON schema shown below.
      tags:
      - Vocabulary
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkflowRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/WorkflowRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/WorkflowRequestRequest'
        required: true
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowResponse'
          description: Workflow results grouped by step
        '400':
          description: Invalid request or unsupported vocabulary
        '401':
          description: Invalid or missing authentication. Provide either Authorization header (Bearer
            token) or X-API-Key header.
  /api/v1/omop/translate/:
    post:
      operationId: v1_omop_translate_create
      description: 'Translate a concept code from one vocabulary into another.


        Targets are restricted to vocabularies actually loaded in this OMOP install. A target that is
        not loaded — ICD-11, for example — is **refused with a 400**, never answered with an empty list:
        empty means "no equivalent exists", which is a different and wrong answer.


        Results are `Maps to` targets filtered to standard concepts in the target vocabulary, ordered
        by concept_id so the choice is deterministic. If the source code has no native concept, the curated
        crosswalk is consulted and `source.found_via` reports that.


        **Supports both JWT (Authorization: Bearer) and API key (X-API-Key) authentication.**'
      summary: Translate a concept between vocabularies (API)
      parameters:
      - in: query
        name: response_format
        schema:
          type: string
          enum:
          - json
          - llm
          default: json
        description: Response format. 'json' (default) returns structured JSON. 'llm' returns compact
          markdown tables (text/markdown) optimized for LLM consumption. When 'llm', the response body
          is markdown text rather than the JSON schema shown below.
      tags:
      - Vocabulary
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TranslateRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TranslateRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TranslateRequestRequest'
        required: true
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TranslateResponse'
          description: Standard equivalents in the target vocabulary
        '400':
          description: Unsupported vocabulary, or bad parameters
        '404':
          description: Source code not found
        '401':
          description: Invalid or missing authentication. Provide either Authorization header (Bearer
            token) or X-API-Key header.
  /api/v1/redact/:
    post:
      operationId: v1_redact_create
      description: "\nReplace PHI in free text with indexed placeholders (`[PERSON_1]`, `[MY_IC_1]`)\n\
        and return the placeholder→original mapping so you can restore values after\nyour own model call.\n\
        \n**The mapping is never stored by us.** It is returned in the response and\ndiscarded; you own\
        \ re-identification. Every response carries a redaction\nreceipt (engine, version, entity counts)\
        \ as the auditable proof that redaction\nran — the receipt records what was caught, and is not\
        \ a claim of perfect\nrecall.\n\nDetection covers Malaysian identifiers (MyKad/IC, MRN, passport,\
        \ local phone\nformats) alongside names, emails, and card/account numbers. Clinical content is\n\
        deliberately preserved: ages, dates like \"day 3 post-op\", vitals, and dosage\nabbreviations\
        \ are NOT redacted, because stripping them destroys the meaning a\ndownstream model needs.\n\n\
        **Fail-closed:** if redaction cannot complete, this endpoint returns 503 with a\n`failed_closed`\
        \ receipt and no text — it never returns your input unredacted.\n    "
      summary: Redact PHI/PII from free text
      tags:
      - Gateway
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RedactRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RedactRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RedactRequestRequest'
        required: true
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RedactResponse'
              examples:
                DischargeSummaryLine:
                  value:
                    redacted_text: '[PERSON_1], IC [MY_IC_1], 72yo day 3 post-op, 500 mg PO amoxicillin'
                    mapping:
                      '[PERSON_1]': Ali bin Ahmad
                      '[MY_IC_1]': 880101-14-5501
                    entities_redacted:
                      PERSON: 1
                      MY_IC: 1
                    redaction_receipt:
                      receipt_version: 1
                      engine: presidio-composed
                      engine_version: '1'
                      rehydration_complete: null
                      unresolved_placeholder_count: 0
                      client_preredacted: false
                      entities_redacted:
                        PERSON: 1
                        MY_IC: 1
                      free_text_fields: 1
                      client_asserted_deidentified: false
                      redacted_anyway: false
                      status: ok
                      retention: metadata_only
                  summary: Discharge summary line
          description: Redacted text plus the mapping and receipt
        '400':
          description: Invalid request
        '401':
          description: Authentication required
        '429':
          description: Rate limit exceeded
        '503':
          description: Redaction failed — request refused
  /api/v1/search/:
    post:
      operationId: v1_search_create
      description: 'Searches OMOP concepts across multiple medical vocabularies (ICD10, SNOMED, CPT4,
        etc.) with optional domain filtering. Returns matching concepts with metadata about the search.


        **Supports both JWT (Authorization: Bearer) and API key (X-API-Key) authentication.**


        Supported vocabularies: ICD10, ICD10CM, ICD10PCS, ICD9CM, ICD9Proc, SNOMED, CPT4, HCPCS, RxNorm,
        RxNorm Extension, OPCS4, DRG, LOINC, ICDO3, ATC.


        A search with no `vocabularies` filter scans ICD10, ICD10CM, ICD10PCS, ICD9CM, ICD9Proc, SNOMED,
        CPT4, HCPCS, RxNorm, RxNorm Extension. ATC, DRG, ICDO3, LOINC, OPCS4 are supported but must be
        requested by name.


        Supported domains: Condition, Procedure, Drug, Measurement, Observation, Device, Specimen.'
      summary: Search OMOP concepts (API)
      parameters:
      - in: query
        name: profile
        schema:
          type: string
          enum:
          - explore
          - hybrid
          - mapping
        description: Profile preset that bundles sensible defaults for a use case. `mapping` — find equivalent
          codes in the requested vocabularies (2-hop graph traversal, vocab-filtered, no bridges). `explore`
          — show the full graph neighborhood (3-hop, includes SNOMED bridge concepts, no vocab filter,
          no quality gate). `hybrid` — plain text search, no graph. Individual params in the request body
          override profile defaults.
      - in: query
        name: q
        schema:
          type: string
        description: Search query string (e.g. 'diabetes', 'appendectomy'). Can also be supplied in the
          request body as `query` (one of `q` or `query` is required).
      - in: query
        name: response_format
        schema:
          type: string
          enum:
          - json
          - llm
          default: json
        description: Response format. 'json' (default) returns structured JSON. 'llm' returns compact
          markdown tables (text/markdown) optimized for LLM consumption. When 'llm', the response body
          is markdown text rather than the JSON schema shown below.
      - in: query
        name: search_mode
        schema:
          type: string
          enum:
          - graph
          - hybrid
          default: hybrid
        description: Search strategy. `hybrid` uses RRF+reranking (dense + sparse fusion); `graph` uses
          OMOP graph traversal for cross-vocabulary and hierarchical expansion. Overridden by the `profile`
          preset when both are set.
      tags:
      - Vocabulary
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SearchOMOPConceptRequestRequest'
            examples:
              HybridSearch(default):
                value:
                  query: diabetes
                  top_k: 5
                summary: Hybrid search (default)
                description: Plain hybrid search across all supported vocabularies.
              GraphSearchWithVocabularyFilter:
                value:
                  query: appendectomy
                  vocabularies:
                  - ICD10CM
                  - ICD10PCS
                  top_k: 10
                  include_bridges: false
                  filter_to_input_vocab: true
                summary: Graph search with vocabulary filter
                description: Graph-native search restricted to ICD10CM/ICD10PCS. Cross-vocab anchor fallback
                  (via SNOMED) and 2-hop Maps-to translation let queries like 'appendectomy' find ICD10PCS/CM
                  equivalents even when the literal term isn't in the target vocabulary.
              MappingProfile(cross-vocabEquivalents):
                value:
                  query: type 2 diabetes mellitus
                  vocabularies:
                  - SNOMED
                  - ICD10CM
                  top_k: 5
                summary: Mapping profile (cross-vocab equivalents)
                description: Use the `mapping` profile to find equivalent codes across vocabularies. Pass
                  `?profile=mapping` as a query-string param alongside this body.
              Query-string-only(q+Profile):
                value: {}
                summary: Query-string-only (q + profile)
                description: Minimal request using only query-string params. The body can be empty when
                  `q` is supplied. Equivalent to the `mapping` example above but without a body.
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/SearchOMOPConceptRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/SearchOMOPConceptRequestRequest'
        required: true
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchOMOPVocabularyResponse'
          description: Search results with metadata
        '400':
          description: Invalid request parameters (missing query, invalid search_mode, invalid vocabularies,
            invalid domain, or top_k out of range)
        '401':
          description: Invalid or missing authentication. Provide either Authorization header (Bearer
            token) or X-API-Key header.
        '500':
          description: Internal server error during search
  /api/v1/search/batch/:
    post:
      operationId: v1_search_batch_create
      description: 'Search many raw terms in a single request. Every term''s query is embedded in ONE
        batched pass, then each term runs the same hybrid pipeline as `POST /api/v1/search/` (dense +
        FTS + RRF + cross-encoder rerank). Results are returned in the same order as the input `terms`
        array, and each term carries a `confidence` signal (top score + margin over the runner-up) so
        callers can gate on match quality.


        Per-term `domain_id`, `vocabularies` and `top_k` override the request-level shared defaults. FTS
        options are shared across the batch. At most 100 terms per request.


        This endpoint always uses hybrid search mode; for graph-mode or relationship expansion use the
        single-query `POST /api/v1/search/`.


        **Supports both JWT (Authorization: Bearer) and API key (X-API-Key) authentication.**'
      summary: Batch search OMOP concepts (API)
      parameters:
      - in: query
        name: response_format
        schema:
          type: string
          enum:
          - json
          - llm
          default: json
        description: Response format. 'json' (default) returns structured JSON. 'llm' returns compact
          markdown tables (text/markdown) optimized for LLM consumption. When 'llm', the response body
          is markdown text rather than the JSON schema shown below.
      tags:
      - Vocabulary
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BatchSearchRequestRequest'
            examples:
              BatchOfRawTerms:
                value:
                  terms:
                  - query: type 2 diabetes
                    domain_id: Condition
                  - query: appendectomy
                    vocabularies:
                    - ICD10PCS
                    top_k: 5
                  - query: metformin
                  vocabularies:
                  - SNOMED
                  - ICD10CM
                  top_k: 10
                summary: Batch of raw terms
                description: Three terms with shared and per-term filters.
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/BatchSearchRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/BatchSearchRequestRequest'
        required: true
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BatchSearchResponse'
          description: Per-term search results (input order) with confidence signals
        '400':
          description: Invalid request (missing/empty terms, too many terms, missing per-term query, invalid
            vocabularies/domain, or top_k out of range)
        '401':
          description: Invalid or missing authentication (Bearer token or X-API-Key).
        '500':
          description: Internal server error during search
  /api/v1/search/domains/:
    get:
      operationId: v1_search_domains_retrieve
      description: 'Returns list of all supported OMOP domains available for concept search.


        **Supports both JWT (Authorization: Bearer) and API key (X-API-Key) authentication.**'
      summary: Get supported domains (API)
      parameters:
      - in: query
        name: response_format
        schema:
          type: string
          enum:
          - json
          - llm
          default: json
        description: Response format. 'json' (default) returns structured JSON. 'llm' returns compact
          markdown tables (text/markdown) optimized for LLM consumption. When 'llm', the response body
          is markdown text rather than the JSON schema shown below.
      tags:
      - Vocabulary
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupportedDomainsResponse'
          description: Supported domains
        '401':
          description: Invalid or missing authentication. Provide either Authorization header (Bearer
            token) or X-API-Key header.
  /api/v1/search/vocabularies/:
    get:
      operationId: v1_search_vocabularies_retrieve
      description: 'Returns lists of all supported medical vocabularies (ICD10, SNOMED, CPT4, etc.) and
        domains (Condition, Procedure, Drug, etc.) available for OMOP concept search.


        **Supports both JWT (Authorization: Bearer) and API key (X-API-Key) authentication.**'
      summary: Get supported vocabularies (API)
      parameters:
      - in: query
        name: response_format
        schema:
          type: string
          enum:
          - json
          - llm
          default: json
        description: Response format. 'json' (default) returns structured JSON. 'llm' returns compact
          markdown tables (text/markdown) optimized for LLM consumption. When 'llm', the response body
          is markdown text rather than the JSON schema shown below.
      tags:
      - Vocabulary
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupportedVocabulariesResponse'
          description: Supported vocabularies and domains
        '401':
          description: Invalid or missing authentication. Provide either Authorization header (Bearer
            token) or X-API-Key header.
  /api/v1/usage/history/:
    get:
      operationId: user_api_usage_history
      description: 'Returns the authenticated user''s individual API request log as a paginated list over
        the selected date range, optionally filtered by service type. Scoped to the caller''s own activity:
        a JWT sees the user''s interactive requests, an API key sees that key''s own requests.'
      summary: Personal API usage history
      parameters:
      - in: query
        name: end_date
        schema:
          type: string
        description: 'End date (ISO format, default: now)'
      - in: query
        name: page
        schema:
          type: integer
        description: 'Page number (default: 1)'
      - in: query
        name: page_size
        schema:
          type: integer
        description: 'Items per page (default: 20, max: 100)'
      - in: query
        name: service_type
        schema:
          type: string
        description: Filter by service type (e.g. "drg_classify", "drg_agent")
      - in: query
        name: start_date
        schema:
          type: string
        description: 'Start date (ISO format, default: 30 days ago)'
      tags:
      - Account
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/UserApiUsageHistory'
          description: ''
  /api/v1/usage/history/{log_id}/:
    get:
      operationId: user_api_usage_detail
      description: Returns the full detail of a single API request log entry made by the authenticated
        user, identified by log ID. Returns 404 if the log does not exist or does not belong to the caller.
      summary: Personal API usage detail
      parameters:
      - in: path
        name: log_id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - Account
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserApiUsageHistory'
          description: ''
        '404':
          description: Not found
  /api/v1/usage/summary/:
    get:
      operationId: user_api_usage_summary
      description: 'Returns the authenticated user''s aggregated API usage (request counts, token totals,
        and cost) over the selected date range, optionally filtered by service type. Scoped to the caller''s
        own activity: a JWT sees the user''s interactive requests, an API key sees that key''s own requests.'
      summary: Personal API usage summary
      parameters:
      - in: query
        name: end_date
        schema:
          type: string
        description: 'End date (ISO format, default: now)'
      - in: query
        name: service_type
        schema:
          type: string
        description: Filter by service type (e.g. "drg_classify", "drg_agent")
      - in: query
        name: start_date
        schema:
          type: string
        description: 'Start date (ISO format, default: 30 days ago)'
      tags:
      - Account
      security:
      - api_key_auth: []
      - jwt_auth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserApiUsageSummary'
          description: ''
components:
  schemas:
    AncestorConcept:
      type: object
      properties:
        concept_id:
          type: integer
        concept_name:
          type: string
        concept_code:
          type: string
        vocabulary_id:
          type: string
        concept_class_id:
          type: string
        min_levels_of_separation:
          type: integer
        max_levels_of_separation:
          type: integer
      required:
      - concept_class_id
      - concept_code
      - concept_id
      - concept_name
      - max_levels_of_separation
      - min_levels_of_separation
      - vocabulary_id
    AncestorsResponse:
      type: object
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/AncestorConcept'
        metadata:
          type: object
          additionalProperties: {}
      required:
      - metadata
      - results
    BatchSearchConfidence:
      type: object
      properties:
        score_source:
          nullable: true
          description: 'Where this term''s similarity scores came from. Only ''cross_encoder'' scores
            are calibrated cross-encoder logits on a consistent scale; ''rrf''/''rank'' are uncalibrated
            fallback scores (used for small candidate sets) whose magnitudes are NOT comparable across
            terms. ''none'' means the term had no results.


            * `cross_encoder` - cross_encoder

            * `rrf` - rrf

            * `rank` - rank

            * `none` - none'
          oneOf:
          - $ref: '#/components/schemas/ScoreSourceEnum'
          - $ref: '#/components/schemas/NullEnum'
        reranked:
          type: boolean
          description: True iff score_source == 'cross_encoder'. When False, the numeric confidence fields
            below are null because fallback scores cannot be safely thresholded.
        top_score:
          type: number
          format: double
          nullable: true
          description: Calibrated cross-encoder similarity of the top concept. Null when the term has
            no results OR was not reranked (see score_source).
        runner_up_score:
          type: number
          format: double
          nullable: true
          description: Calibrated cross-encoder similarity of the second concept. Null when fewer than
            2 results OR the term was not reranked.
        margin:
          type: number
          format: double
          nullable: true
          description: top_score - runner_up_score. A large positive margin means a confident, unambiguous
            top match. Only meaningful when reranked=True; null otherwise (fewer than 2 results or not
            reranked). Gate acceptance on this ONLY when reranked is True.
      required:
      - margin
      - reranked
      - runner_up_score
      - score_source
      - top_score
    BatchSearchRequestRequest:
      type: object
      description: 'Request body for POST /api/v1/search/batch/.


        Embeds every term''s query in a single batched pass, then runs the standard

        per-term hybrid search (dense + FTS + RRF + rerank). Per-term `domain_id`,

        `vocabularies` and `top_k` override the request-level shared defaults.'
      properties:
        terms:
          type: array
          items:
            $ref: '#/components/schemas/BatchSearchTermRequest'
          description: 1-100 terms to search. Order is preserved in the response.
          maxItems: 100
          minItems: 1
        vocabularies:
          type: array
          items:
            type: string
            minLength: 1
          description: Default vocabulary filter for terms that don't specify their own.
        domain_id:
          type: string
          minLength: 1
          description: Default domain filter for terms that don't specify their own.
        top_k:
          type: integer
          maximum: 100
          minimum: 1
          default: 10
          description: Default result cap for terms that don't specify their own (1-100).
        search_type:
          allOf:
          - $ref: '#/components/schemas/SearchTypeEnum'
          default: websearch
          description: 'FTS search type applied to every term.


            * `plain` - plain

            * `phrase` - phrase

            * `raw` - raw

            * `websearch` - websearch'
        language:
          type: string
          minLength: 1
          default: english
          description: PostgreSQL text search configuration applied to every term.
        use_search_rank:
          type: boolean
          default: true
          description: Use SearchRank ordering for FTS results.
        rank_normalization:
          type: integer
          default: 0
          description: SearchRank normalization flags (0-31).
        min_relevance:
          type: number
          format: double
          nullable: true
          default: -2.0
          description: Cross-encoder logit threshold applied to every term. Results below this are dropped.
            Pass null to disable filtering.
      required:
      - terms
    BatchSearchResponse:
      type: object
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/BatchSearchTermResult'
          description: Per-term results, in the same order as the input `terms` array.
        metadata:
          type: object
          additionalProperties: {}
          description: Batch-level metadata (term count, search mode, shared FTS options).
      required:
      - metadata
      - results
    BatchSearchTermRequest:
      type: object
      description: A single term in a batch concept-search request.
      properties:
        query:
          type: string
          minLength: 1
          description: Raw term to search (e.g., 'type 2 diabetes').
        domain_id:
          type: string
          minLength: 1
          description: Per-term domain filter (e.g., 'Condition'). Overrides no shared default.
        vocabularies:
          type: array
          items:
            type: string
            minLength: 1
          description: Per-term vocabulary filter (case-insensitive). Falls back to the request-level
            `vocabularies`, then to all supported vocabularies.
        top_k:
          type: integer
          maximum: 100
          minimum: 1
          description: Per-term result cap (1-100). Falls back to request-level `top_k`.
      required:
      - query
    BatchSearchTermResult:
      type: object
      properties:
        query:
          type: string
          description: Echo of the input term query.
        domain_id:
          type: string
          nullable: true
          description: Domain filter actually applied for this term.
        vocabularies:
          type: array
          items:
            type: string
          description: Vocabularies actually searched for this term (DB casing).
        top_k:
          type: integer
          description: Result cap actually applied for this term.
        concepts:
          type: array
          items:
            $ref: '#/components/schemas/OMOPConcept'
          description: Ranked matches, same shape as the single-query endpoint.
        confidence:
          allOf:
          - $ref: '#/components/schemas/BatchSearchConfidence'
          description: Per-term confidence signal derived from the top two similarities.
      required:
      - concepts
      - confidence
      - domain_id
      - query
      - top_k
      - vocabularies
    ChatCompletionRequestRequest:
      type: object
      properties:
        model:
          type: string
          minLength: 1
          description: A model id from the registry. External keys may only use models approved for PHI
            traffic.
        messages:
          type: array
          items:
            $ref: '#/components/schemas/ChatMessageRequest'
        max_tokens:
          type: integer
          maximum: 32000
          minimum: 1
          default: 1024
        temperature:
          type: number
          format: double
          nullable: true
        top_p:
          type: number
          format: double
          nullable: true
        stop:
          type: string
          nullable: true
          minLength: 1
        stream:
          type: boolean
          default: false
        reasoning:
          type: object
          additionalProperties: {}
          nullable: true
          description: 'Reasoning-model controls, e.g. {"effort": "high", "summary": "detailed"}. Only
            `effort` and `summary` are forwarded; other keys are ignored. Applies to the GPT-5.x family;
            ignored by other models.'
        redact_only:
          type: boolean
          default: false
          description: Return the model's answer WITHOUT substituting your values back (placeholders remain).
            Required if you want streaming, and useful when the answer is going somewhere that should
            not hold PHI.
        tools:
          type: array
          items:
            type: object
            additionalProperties: {}
          description: OpenAI-style function tools. Forwarded to the model; returned tool_calls come back
            with your values re-hydrated.
        tool_choice:
          description: '"auto" (default) or "none". Forcing a specific call ("required", or a named function)
            is refused rather than downgraded to "auto", so a forced call never appears to have been honoured
            when it was not.'
        conversation_key:
          type: string
          description: Supply the same value across turns so a given person keeps the same placeholder;
            multi-turn clients resend history and renaming mid-conversation confuses the model.
          maxLength: 200
      required:
      - messages
      - model
    ChatMessageRequest:
      type: object
      properties:
        role:
          $ref: '#/components/schemas/ChatMessageRoleEnum'
        content:
          type: string
          nullable: true
          maxLength: 100000
        tool_calls:
          type: array
          items:
            type: object
            additionalProperties: {}
        tool_call_id:
          type: string
          minLength: 1
          description: 'Required on a `tool` message: it matches the result to the call that asked for
            it.'
          maxLength: 200
      required:
      - role
    ChatMessageRoleEnum:
      enum:
      - system
      - developer
      - user
      - assistant
      - tool
      type: string
      description: '* `system` - system

        * `developer` - developer

        * `user` - user

        * `assistant` - assistant

        * `tool` - tool'
    CodeRequestDomainEnum:
      enum:
      - Condition
      - Procedure
      - Drug
      - Measurement
      - Observation
      - Device
      - Specimen
      type: string
      description: '* `Condition` - Condition

        * `Procedure` - Procedure

        * `Drug` - Drug

        * `Measurement` - Measurement

        * `Observation` - Observation

        * `Device` - Device

        * `Specimen` - Specimen'
    CodeRequestRequest:
      type: object
      description: 'Input to `/code`.


        Deliberately small. Routing (graph vs hybrid, and whether to narrow by

        domain) is NOT a request parameter — it comes from the vocabulary

        capability registry, which is the point of the endpoint. `domain` is the

        one override, for a caller who genuinely knows better than the registry.'
      properties:
        text:
          type: string
          minLength: 1
          description: Clinical text to resolve, e.g. 'lap appendicectomy'.
        vocabulary:
          allOf:
          - $ref: '#/components/schemas/VocabularyEnum'
          description: 'Target vocabulary. Every choice here is served; see the `routing.evidence` field
            in the response for how well each one''s retrieval has been validated.


            * `ICD10CM` - ICD10CM

            * `ICD10PCS` - ICD10PCS

            * `CPT4` - CPT4

            * `RxNorm` - RxNorm

            * `SNOMED` - SNOMED'
        domain:
          nullable: true
          description: 'Override the OMOP domain filter. Three distinct inputs:


            - **omit** — the vocabulary decides. ICD10PCS narrows to Procedure and RxNorm to Drug, because
            a filter excludes ~0% of each. ICD10CM, CPT4 and SNOMED stay unfiltered, because narrowing
            them hides billable concepts behind plausible wrong answers.

            - **a domain** — filter by it, whatever the vocabulary implies.

            - **null** — do NOT filter, even where the vocabulary implies a domain. This is the only way
            to opt out of an implied filter.


            * `Condition` - Condition

            * `Procedure` - Procedure

            * `Drug` - Drug

            * `Measurement` - Measurement

            * `Observation` - Observation

            * `Device` - Device

            * `Specimen` - Specimen'
          oneOf:
          - $ref: '#/components/schemas/CodeRequestDomainEnum'
          - $ref: '#/components/schemas/NullEnum'
        top_k:
          type: integer
          maximum: 100
          minimum: 1
          default: 10
          description: Number of concepts to return (1-100).
      required:
      - text
      - vocabulary
    CodeResponse:
      type: object
      properties:
        results:
          type: array
          items:
            type: object
            additionalProperties: {}
        routing:
          $ref: '#/components/schemas/CodeRouting'
        metadata:
          type: object
          additionalProperties: {}
      required:
      - metadata
      - results
      - routing
    CodeRouting:
      type: object
      description: What the API decided, and how much it knows about that decision.
      properties:
        vocabulary:
          type: string
        search_mode:
          type: string
          description: 'Retrieval route chosen from the registry: ''graph'' or ''hybrid''.'
        effective_search_mode:
          type: string
          description: Present only when the engine degraded the chosen mode. Reports what actually ran,
            so `search_mode` is never a statement about intent alone.
        fallback_reason:
          type: string
        domain_filter:
          type: string
          nullable: true
          description: Domain applied, or null if unfiltered.
        domain_source:
          type: string
          nullable: true
          description: '''caller'' if overridden, ''vocabulary'' if implied, null if none.'
        evidence:
          type: string
          description: 'How well this routing is validated: ''strong'' (a leak-free gold set), ''structural''
            (forced by a property of the data), ''weak'' (small-n or retracted), ''none'' (never measured).'
        measured:
          type: boolean
          description: False when evidence is 'none'. The answer is served, not validated.
        caveat:
          type: string
          description: Present only when `measured` is false.
      required:
      - domain_filter
      - domain_source
      - evidence
      - measured
      - search_mode
      - vocabulary
    DRGAgentRequestRequest:
      type: object
      description: Request serializer for the DRG AI agent endpoint.
      properties:
        message:
          type: string
          minLength: 1
          description: Natural language clinical text or DRG question (max 20,000 characters)
          maxLength: 20000
        version:
          allOf:
          - $ref: '#/components/schemas/VersionEnum'
          description: 'Optional MS-DRG version hint for the agent


            * `v35` - v35

            * `v36` - v36

            * `v37` - v37

            * `v38` - v38

            * `v39` - v39

            * `v40` - v40

            * `v43` - v43'
        deidentified:
          type: boolean
          default: false
          description: Whether the input clinical text has already been de-identified
      required:
      - message
    DRGAgentResponse:
      type: object
      description: Response serializer for the DRG AI agent endpoint.
      properties:
        response:
          type: string
          description: Agent's natural language response
        token_usage:
          type: object
          additionalProperties: {}
          description: Token usage breakdown from the LLM
        response_time_ms:
          type: integer
          description: Response time in milliseconds
      required:
      - response
      - response_time_ms
      - token_usage
    DRGBatchCaseRequest:
      type: object
      description: One case inside a batch submission.
      properties:
        case_ref:
          type: string
          minLength: 1
          description: Your identifier for this case; unique within the batch — results are returned keyed
            on it.
          maxLength: 128
        dx_codes:
          type: array
          items:
            type: string
            minLength: 1
            maxLength: 10
          description: ICD-10-CM diagnosis codes. First code is principal diagnosis.
          minItems: 1
        pr_codes:
          type: array
          items:
            type: string
            minLength: 1
            maxLength: 10
          default: []
          description: ICD-10-PCS procedure codes (7 characters each).
        gender:
          allOf:
          - $ref: '#/components/schemas/GenderEnum'
          default: F
        is_alive:
          type: boolean
          default: true
      required:
      - case_ref
      - dx_codes
    DRGBatchRequestRequest:
      type: object
      description: Request serializer for the async batch classification endpoint.
      properties:
        cases:
          type: array
          items:
            $ref: '#/components/schemas/DRGBatchCaseRequest'
          description: Up to 1000 cases per submission.
          maxItems: 1000
          minItems: 1
        source_dialect:
          allOf:
          - $ref: '#/components/schemas/SourceDialectEnum'
          default: icd10cm
          description: 'Code dialect of the submitted cases. Only icd10cm is accepted today; WHO ICD-10
            / ICD-11 batches are refused up front rather than mis-grouped.


            * `icd10cm` - icd10cm'
        version:
          allOf:
          - $ref: '#/components/schemas/VersionEnum'
          default: v40
          description: 'MS-DRG version (default: v40, FY 2023)


            * `v35` - v35

            * `v36` - v36

            * `v37` - v37

            * `v38` - v38

            * `v39` - v39

            * `v40` - v40

            * `v43` - v43'
      required:
      - cases
    DRGClassifyInput:
      type: object
      description: Serializer for the input echo in DRG classification response.
      properties:
        dx_codes:
          type: array
          items:
            type: string
        pr_codes:
          type: array
          items:
            type: string
        gender:
          type: string
        is_alive:
          type: boolean
      required:
      - dx_codes
      - gender
      - is_alive
      - pr_codes
    DRGClassifyRequestRequest:
      type: object
      description: Request serializer for direct DRG classification endpoint.
      properties:
        dx_codes:
          type: array
          items:
            type: string
            minLength: 1
            maxLength: 10
          description: ICD-10-CM diagnosis codes. First code is principal diagnosis.
          minItems: 1
        pr_codes:
          type: array
          items:
            type: string
            minLength: 1
            maxLength: 10
          default: []
          description: ICD-10-PCS procedure codes (7 characters each).
        gender:
          allOf:
          - $ref: '#/components/schemas/GenderEnum'
          default: F
          description: 'Patient gender: "F" (female) or "M" (male)


            * `F` - F

            * `M` - M'
        is_alive:
          type: boolean
          default: true
          description: Whether the patient was discharged alive
        version:
          allOf:
          - $ref: '#/components/schemas/VersionEnum'
          default: v40
          description: 'MS-DRG version (default: v40, FY 2023)


            * `v35` - v35

            * `v36` - v36

            * `v37` - v37

            * `v38` - v38

            * `v39` - v39

            * `v40` - v40

            * `v43` - v43'
      required:
      - dx_codes
    DRGClassifyResponse:
      type: object
      description: Response serializer for direct DRG classification endpoint.
      properties:
        primary_drg:
          type: string
          description: Primary DRG code (3-digit string)
        description:
          type: string
          description: DRG description
        mdc:
          type: string
          description: Major Diagnostic Category
        type:
          type: string
          description: 'DRG type: M (Medical) or P (Procedural)'
        all_candidates:
          type: array
          items:
            type: string
          description: All candidate DRG codes
        version:
          type: string
          description: MS-DRG version used
        fiscal_year:
          type: string
          description: Fiscal year for the version
        input:
          allOf:
          - $ref: '#/components/schemas/DRGClassifyInput'
          description: Echo of the input parameters
        response_time_ms:
          type: integer
          description: Response time in milliseconds
      required:
      - all_candidates
      - description
      - fiscal_year
      - input
      - mdc
      - primary_drg
      - response_time_ms
      - type
      - version
    DescendantsResponse:
      type: object
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/AncestorConcept'
        metadata:
          type: object
          additionalProperties: {}
      required:
      - metadata
      - results
    ExpandRequestRequest:
      type: object
      properties:
        query:
          type: string
          minLength: 1
          description: Clinical term or concept to expand (e.g., 'eczema', 'diabetes')
        vocabulary_ids:
          type: array
          items:
            type: string
            minLength: 1
          description: Vocabularies to expand within (e.g., ['ICD10CM', 'ICD10'])
          minItems: 1
        domain_id:
          type: string
          minLength: 1
          description: Filter by domain (e.g., 'Condition', 'Procedure')
        max_ancestor_separation:
          type: integer
          maximum: 10
          minimum: 1
          default: 2
          description: Max hierarchy levels to ascend (default 2)
        max_descendant_separation:
          type: integer
          maximum: 10
          minimum: 1
          default: 4
          description: Max hierarchy levels to descend (default 4)
        max_concepts:
          type: integer
          maximum: 5000
          minimum: 1
          default: 500
          description: Maximum concepts in result set (default 500)
        include_mappings:
          type: boolean
          default: false
          description: Include cross-vocabulary mappings for each concept
        target_vocabularies:
          type: array
          items:
            type: string
            minLength: 1
          description: Target vocabularies for mappings (e.g., ['SNOMED'])
      required:
      - query
      - vocabulary_ids
    ExpandResponse:
      type: object
      properties:
        results:
          $ref: '#/components/schemas/ExpandResults'
        metadata:
          type: object
          additionalProperties: {}
      required:
      - metadata
      - results
    ExpandResults:
      type: object
      properties:
        concept_ids:
          type: array
          items:
            type: integer
        concepts:
          type: array
          items:
            $ref: '#/components/schemas/ExpandedConcept'
      required:
      - concept_ids
      - concepts
    ExpandedConcept:
      type: object
      properties:
        concept_id:
          type: integer
        concept_name:
          type: string
        concept_code:
          type: string
        vocabulary_id:
          type: string
        concept_class_id:
          type: string
          nullable: true
      required:
      - concept_class_id
      - concept_code
      - concept_id
      - concept_name
      - vocabulary_id
    GenderEnum:
      enum:
      - F
      - M
      type: string
      description: '* `F` - F

        * `M` - M'
    GetAncestorsRequestRequest:
      type: object
      properties:
        concept_id:
          type: integer
          description: Source concept ID
        max_separation:
          type: integer
          minimum: 1
          nullable: true
          description: Maximum levels of separation (null for unlimited)
        vocabulary_id:
          type: string
          minLength: 1
          description: Filter ancestors by vocabulary
        include_paths:
          type: boolean
          default: true
          description: Include ordered hierarchy chains (direct parent -> root) in the response. Paths
            ignore the vocabulary_id filter since chains may cross vocabularies.
      required:
      - concept_id
    GetDescendantsRequestRequest:
      type: object
      properties:
        concept_id:
          type: integer
          description: Source concept ID
        max_separation:
          type: integer
          minimum: 1
          nullable: true
          description: Maximum levels of separation (null for unlimited)
        vocabulary_id:
          type: string
          minLength: 1
          description: Filter descendants by vocabulary
        include_paths:
          type: boolean
          default: true
          description: Include ordered hierarchy chains (direct child -> leaf) in the response. Paths
            ignore the vocabulary_id filter since chains may cross vocabularies.
      required:
      - concept_id
    GetRelationshipsRequestRequest:
      type: object
      properties:
        concept_id:
          type: integer
          description: Source concept ID
      required:
      - concept_id
    MapConceptRequestRequest:
      type: object
      properties:
        concept_id:
          type: integer
          description: Source concept ID to map
        target_vocabularies:
          type: array
          items:
            type: string
            minLength: 1
          description: Target vocabularies for mapping (e.g., ['SNOMED', 'ICD10CM'])
        relationship_ids:
          type: array
          items:
            type: string
            minLength: 1
          description: 'Relationship types to follow (default: [''Maps to''])'
      required:
      - concept_id
    MapResponse:
      type: object
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/MappedConcept'
        metadata:
          type: object
          additionalProperties: {}
      required:
      - metadata
      - results
    MappedConcept:
      type: object
      properties:
        concept_id:
          type: integer
        concept_name:
          type: string
        concept_code:
          type: string
        vocabulary_id:
          type: string
        concept_class_id:
          type: string
        relationship_id:
          type: string
      required:
      - concept_class_id
      - concept_code
      - concept_id
      - concept_name
      - relationship_id
      - vocabulary_id
    NormalizeRequestRequest:
      type: object
      properties:
        query:
          type: string
          minLength: 1
          description: Search query to normalize
        vocabularies:
          type: array
          items:
            type: string
            minLength: 1
          description: Vocabularies to search (e.g., ['SNOMED', 'ICD10CM'])
        domain_id:
          type: string
          minLength: 1
          description: Filter by domain (e.g., 'Condition')
        concept_class_id:
          type: string
          minLength: 1
          description: Filter by concept class (e.g., 'Clinical Finding', 'Ingredient')
        top_k:
          type: integer
          maximum: 100
          minimum: 1
          default: 5
          description: Number of results to return (1-100)
        sort_by:
          allOf:
          - $ref: '#/components/schemas/OmopSortByEnum'
          default: score
          description: 'Sort results by: ''score'' (relevance, default) or ''concept_class_id'' (alphabetical)


            * `score` - score

            * `concept_class_id` - concept_class_id'
      required:
      - query
    NormalizeResponse:
      type: object
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/ResolvedConcept'
        metadata:
          type: object
          additionalProperties: {}
      required:
      - metadata
      - results
    NullEnum:
      enum:
      - null
    OMOPConcept:
      type: object
      properties:
        concept_id:
          type: integer
        concept_name:
          type: string
        concept_code:
          type: string
        vocabulary_id:
          type: string
        concept_class_id:
          type: string
          nullable: true
        similarity:
          type: number
          format: double
          nullable: true
          description: Final relevance score from cross-encoder reranking (both modes).
        graph_score:
          type: number
          format: double
          nullable: true
          description: Normalised graph-proximity score in [0, 1]. Present only when search_mode=graph.
        combined_score:
          type: number
          format: double
          nullable: true
          description: Deprecated alias of `graph_score` — the two are always equal. Kept so existing
            graph-mode callers do not break. This field once documented a weighted graph/text blend that
            the code never computed; read `graph_score` for the graph signal and `score` for the cross-encoder
            relevance. Present only when search_mode=graph.
        score:
          type: number
          format: double
          nullable: true
          description: Alias for similarity after rerank. Present only when search_mode=graph.
        is_bridge:
          type: boolean
          description: Present and True when this concept is a 2-hop Maps-to bridge (typically a SNOMED
            standard concept) surfaced via include_bridges=True. Absent on direct matches and translated
            in-vocab equivalents.
        parents:
          type: array
          items:
            $ref: '#/components/schemas/ParentConcept'
          nullable: true
      required:
      - concept_class_id
      - concept_code
      - concept_id
      - concept_name
      - vocabulary_id
    OmopSortByEnum:
      enum:
      - score
      - concept_class_id
      type: string
      description: '* `score` - score

        * `concept_class_id` - concept_class_id'
    ParentConcept:
      type: object
      properties:
        parent_concept_id:
          type: integer
        parent_concept_code:
          type: string
        parent_concept_name:
          type: string
      required:
      - parent_concept_code
      - parent_concept_id
      - parent_concept_name
    RedactRequestRequest:
      type: object
      properties:
        text:
          type: string
          minLength: 1
          description: Free text to redact. PHI is replaced with indexed placeholders.
          maxLength: 100000
        prior_mapping:
          type: object
          additionalProperties:
            type: string
          description: 'Optional, and the preferred way to keep placeholders stable. Send back the `mapping`
            a previous call returned: a value already in it keeps its placeholder, and a new value gets
            the next free one. Supersedes `conversation_key`: if you send both, this one is used.'
        conversation_key:
          type: string
          description: Deprecated in favour of `prior_mapping`. Supply the same value across related calls
            and the same source value keeps the same placeholder. It does this by deriving the placeholder
            from the source value itself, which makes the placeholder a code derived from information
            about the individual, and which cannot detect a collision across separate calls. Prefer `prior_mapping`.
          maxLength: 200
        return_mapping:
          type: boolean
          default: true
          description: Whether to return the placeholder→original mapping. The mapping is never stored
            server-side; set false if you only need redacted text and do not intend to re-identify.
      required:
      - text
    RedactResponse:
      type: object
      description: Documents the response shape for the schema; not used for validation.
      properties:
        redacted_text:
          type: string
        mapping:
          type: object
          additionalProperties:
            type: string
          description: 'placeholder → original value. Returned to YOU and never persisted by us: you own
            re-identification. Omitted when return_mapping=false.'
        entities_redacted:
          type: object
          additionalProperties:
            type: integer
        redaction_receipt:
          type: object
          additionalProperties: {}
      required:
      - entities_redacted
      - mapping
      - redacted_text
      - redaction_receipt
    RelationshipsResponse:
      type: object
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/MappedConcept'
        metadata:
          type: object
          additionalProperties: {}
      required:
      - metadata
      - results
    ResolveRequestRequest:
      type: object
      properties:
        query:
          type: string
          minLength: 1
          description: Search query to resolve
        vocabularies:
          type: array
          items:
            type: string
            minLength: 1
          description: Vocabularies to search
        domain_id:
          type: string
          minLength: 1
          description: Filter by domain
        concept_class_id:
          type: string
          minLength: 1
          description: Filter by concept class (e.g., 'Clinical Finding', 'Ingredient')
        top_k:
          type: integer
          maximum: 100
          minimum: 1
          default: 5
          description: Number of results to return (1-100)
        sort_by:
          allOf:
          - $ref: '#/components/schemas/OmopSortByEnum'
          default: score
          description: 'Sort results by: ''score'' (relevance, default) or ''concept_class_id'' (alphabetical)


            * `score` - score

            * `concept_class_id` - concept_class_id'
        include_ancestors:
          type: boolean
          default: true
          description: Include ancestor concepts
        include_descendants:
          type: boolean
          default: false
          description: Include descendant concepts
        include_mappings:
          type: boolean
          default: true
          description: Include cross-vocabulary mappings
        max_separation:
          type: integer
          minimum: 1
          nullable: true
          description: Maximum hierarchy levels for ancestors/descendants
        target_vocabularies:
          type: array
          items:
            type: string
            minLength: 1
          description: Target vocabularies for mappings
        include_relationships:
          type: boolean
          default: false
          description: Include concept relationships (e.g. 'Is a', 'Subsumes')
      required:
      - query
    ResolveResponse:
      type: object
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/ResolvedConcept'
        metadata:
          type: object
          additionalProperties: {}
      required:
      - metadata
      - results
    ResolvedConcept:
      type: object
      properties:
        concept_id:
          type: integer
        concept_name:
          type: string
        concept_code:
          type: string
        vocabulary_id:
          type: string
        concept_class_id:
          type: string
          nullable: true
        score:
          type: number
          format: double
          nullable: true
        ancestors:
          type: array
          items:
            $ref: '#/components/schemas/AncestorConcept'
          nullable: true
        descendants:
          type: array
          items:
            $ref: '#/components/schemas/AncestorConcept'
          nullable: true
        mappings:
          type: array
          items:
            $ref: '#/components/schemas/MappedConcept'
          nullable: true
        relationships:
          type: array
          items:
            $ref: '#/components/schemas/MappedConcept'
          nullable: true
      required:
      - concept_class_id
      - concept_code
      - concept_id
      - concept_name
      - score
      - vocabulary_id
    ScoreSourceEnum:
      enum:
      - cross_encoder
      - rrf
      - rank
      - none
      type: string
      description: '* `cross_encoder` - cross_encoder

        * `rrf` - rrf

        * `rank` - rank

        * `none` - none'
    SearchFTSOptionsMetadata:
      type: object
      properties:
        search_type:
          type: string
          description: 'FTS search type used: ''plain'', ''phrase'', ''raw'', or ''websearch''.'
        language:
          type: string
          description: PostgreSQL text search configuration used (e.g., 'english').
        use_search_rank:
          type: boolean
          description: Whether SearchRank ordering was applied.
        rank_normalization:
          type: integer
          description: SearchRank normalization flags used (0-31).
      required:
      - language
      - rank_normalization
      - search_type
      - use_search_rank
    SearchMetadata:
      type: object
      properties:
        query:
          type: string
          description: The query string that was searched.
        vocabularies_searched:
          type: array
          items:
            type: string
          description: Vocabularies actually searched (normalized to DB casing).
        domain_id:
          type: string
          nullable: true
          description: Domain filter applied, or null if no domain filter.
        total_results:
          type: integer
          description: Number of results returned in this response.
        find_relationship:
          type: boolean
          description: Whether 'Is a' parent relationships were fetched.
        search_mode:
          allOf:
          - $ref: '#/components/schemas/SearchModeEnum'
          description: 'Effective search mode used. May differ from `requested_search_mode` when graph
            mode fell back to hybrid — see `fallback_reason`.


            * `hybrid` - hybrid

            * `graph` - graph'
        requested_search_mode:
          allOf:
          - $ref: '#/components/schemas/SearchModeEnum'
          description: 'Search mode the caller asked for (before any fallback).


            * `hybrid` - hybrid

            * `graph` - graph'
        profile:
          type: string
          nullable: true
          description: Profile preset applied, or null if no profile was requested.
        include_bridges:
          type: boolean
          description: Whether SNOMED bridge concepts were surfaced (graph mode).
        filter_to_input_vocab:
          type: boolean
          description: Whether results were post-filtered to the requested vocabularies (graph mode).
        fts_options:
          allOf:
          - $ref: '#/components/schemas/SearchFTSOptionsMetadata'
          description: Full-text-search options that were applied.
        fallback_reason:
          type: string
          description: Present only when `search_mode != requested_search_mode`. Explains why graph mode
            fell back to hybrid (e.g., no anchors, low-quality rerank, empty graph expansion).
      required:
      - domain_id
      - filter_to_input_vocab
      - find_relationship
      - fts_options
      - include_bridges
      - profile
      - query
      - requested_search_mode
      - search_mode
      - total_results
      - vocabularies_searched
    SearchModeEnum:
      enum:
      - hybrid
      - graph
      type: string
      description: '* `hybrid` - hybrid

        * `graph` - graph'
    SearchOMOPConceptRequestRequest:
      type: object
      properties:
        query:
          type: string
          minLength: 1
          description: Search query (e.g., 'diabetes', 'appendectomy')
        vocabularies:
          type: array
          items:
            type: string
            minLength: 1
          description: List of vocabularies to search (e.g., ['ICD10', 'SNOMED']). If not provided, searches
            all.
        domain_id:
          type: string
          minLength: 1
          description: Filter by domain (e.g., 'Condition', 'Procedure')
        top_k:
          type: integer
          maximum: 100
          minimum: 1
          default: 10
          description: Number of results to return (1-100)
        find_relationship:
          type: boolean
          default: false
          description: If true, fetch 'Is a' relationships (parent concepts) for each result
        relationship_filters:
          type: object
          additionalProperties: {}
          description: 'Additional filters for ConceptRelationship query. Allowed keys: concept_id_2__concept_class_id,
            concept_id_2__concept_class_id__in, concept_id_2__vocabulary_id, concept_id_2__vocabulary_id__in,
            concept_id_2__domain_id, concept_id_2__domain_id__in'
        search_type:
          allOf:
          - $ref: '#/components/schemas/SearchTypeEnum'
          default: websearch
          description: 'FTS search type: plain (simple word match), phrase (exact phrase), raw (tsquery
            syntax), websearch (web-style with +/-)


            * `plain` - plain

            * `phrase` - phrase

            * `raw` - raw

            * `websearch` - websearch'
        language:
          type: string
          minLength: 1
          default: english
          description: PostgreSQL text search configuration (e.g., 'english', 'simple', 'french')
        use_search_rank:
          type: boolean
          default: true
          description: Use SearchRank for ordering FTS results by relevance
        rank_normalization:
          type: integer
          default: 0
          description: SearchRank normalization flags (0-31). See PostgreSQL docs for details.
        min_relevance:
          type: number
          format: double
          nullable: true
          default: -2.0
          description: Cross-encoder logit threshold. Results scoring below this are dropped before being
            returned. ms-marco-MiniLM-L-6-v2 produces logits roughly in [-15, +15]; ~0 is the irrelevant/relevant
            boundary. Default -2.0 is permissive but blocks the 'no good match anywhere' case. Pass null
            to disable filtering and always return up to top_k results regardless of quality.
        include_bridges:
          type: boolean
          default: false
          description: 'Graph mode only. When True, surface the SNOMED-style standard ''bridge'' concepts
            used for 2-hop Maps-to traversal (each marked `is_bridge: true` in the response). Useful for
            showing the user *why* a non-standard code was suggested. Default False.'
        filter_to_input_vocab:
          type: boolean
          default: true
          description: Graph mode only. When True (default), restrict results to the vocabularies in the
            `vocabularies` filter. Set False to allow cross-vocab graph neighbors (e.g. for an LLM-agent
            profile).
      required:
      - query
    SearchOMOPVocabularyResponse:
      type: object
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/OMOPConcept'
        metadata:
          $ref: '#/components/schemas/SearchMetadata'
      required:
      - metadata
      - results
    SearchTypeEnum:
      enum:
      - plain
      - phrase
      - raw
      - websearch
      type: string
      description: '* `plain` - plain

        * `phrase` - phrase

        * `raw` - raw

        * `websearch` - websearch'
    SourceDialectEnum:
      enum:
      - icd10cm
      type: string
      description: '* `icd10cm` - icd10cm'
    SourceVocabularyEnum:
      enum:
      - ICD10
      - ICD10CM
      - ICD10PCS
      - ICD9CM
      - ICD9Proc
      - SNOMED
      - CPT4
      - HCPCS
      - RxNorm
      - RxNorm Extension
      - OPCS4
      - DRG
      - LOINC
      - ICDO3
      - ATC
      type: string
      description: '* `ICD10` - ICD10

        * `ICD10CM` - ICD10CM

        * `ICD10PCS` - ICD10PCS

        * `ICD9CM` - ICD9CM

        * `ICD9Proc` - ICD9Proc

        * `SNOMED` - SNOMED

        * `CPT4` - CPT4

        * `HCPCS` - HCPCS

        * `RxNorm` - RxNorm

        * `RxNorm Extension` - RxNorm Extension

        * `OPCS4` - OPCS4

        * `DRG` - DRG

        * `LOINC` - LOINC

        * `ICDO3` - ICDO3

        * `ATC` - ATC'
    SupportedDomainsResponse:
      type: object
      properties:
        domains:
          type: array
          items:
            type: string
      required:
      - domains
    SupportedVocabulariesResponse:
      type: object
      properties:
        vocabularies:
          type: array
          items:
            type: string
        domains:
          type: array
          items:
            type: string
      required:
      - domains
      - vocabularies
    TargetVocabularyEnum:
      enum:
      - ICD10
      - ICD10CM
      - ICD10PCS
      - ICD9CM
      - ICD9Proc
      - SNOMED
      - CPT4
      - HCPCS
      - RxNorm
      - RxNorm Extension
      - OPCS4
      - DRG
      - LOINC
      - ICDO3
      - ATC
      type: string
      description: '* `ICD10` - ICD10

        * `ICD10CM` - ICD10CM

        * `ICD10PCS` - ICD10PCS

        * `ICD9CM` - ICD9CM

        * `ICD9Proc` - ICD9Proc

        * `SNOMED` - SNOMED

        * `CPT4` - CPT4

        * `HCPCS` - HCPCS

        * `RxNorm` - RxNorm

        * `RxNorm Extension` - RxNorm Extension

        * `OPCS4` - OPCS4

        * `DRG` - DRG

        * `LOINC` - LOINC

        * `ICDO3` - ICDO3

        * `ATC` - ATC'
    TranslateConcept:
      type: object
      properties:
        concept_id:
          type: integer
        concept_name:
          type: string
        concept_code:
          type: string
        vocabulary_id:
          type: string
        concept_class_id:
          type: string
          nullable: true
        standard_concept:
          type: string
          nullable: true
      required:
      - concept_class_id
      - concept_code
      - concept_id
      - concept_name
      - standard_concept
      - vocabulary_id
    TranslateRequestRequest:
      type: object
      description: 'Input to `/translate`.


        Both vocabularies are ChoiceFields over the LOADED partitions, so an

        unloaded target (ICD-11) is refused at validation with a 400 naming the

        supported set. That refusal is the feature: answering with an empty list

        would say "no equivalent exists", which is a different and wrong answer.'
      properties:
        concept_code:
          type: string
          minLength: 1
          description: Source concept code, e.g. 'K35.80'.
        source_vocabulary:
          allOf:
          - $ref: '#/components/schemas/SourceVocabularyEnum'
          description: 'Vocabulary the code belongs to.


            * `ICD10` - ICD10

            * `ICD10CM` - ICD10CM

            * `ICD10PCS` - ICD10PCS

            * `ICD9CM` - ICD9CM

            * `ICD9Proc` - ICD9Proc

            * `SNOMED` - SNOMED

            * `CPT4` - CPT4

            * `HCPCS` - HCPCS

            * `RxNorm` - RxNorm

            * `RxNorm Extension` - RxNorm Extension

            * `OPCS4` - OPCS4

            * `DRG` - DRG

            * `LOINC` - LOINC

            * `ICDO3` - ICDO3

            * `ATC` - ATC'
        target_vocabulary:
          allOf:
          - $ref: '#/components/schemas/TargetVocabularyEnum'
          description: 'Vocabulary to translate into. Must be loaded in this OMOP install. ICD-11 is NOT
            loaded — it exists here only as a rulebook, not a mapped vocabulary — so it is refused rather
            than answered with nothing.


            * `ICD10` - ICD10

            * `ICD10CM` - ICD10CM

            * `ICD10PCS` - ICD10PCS

            * `ICD9CM` - ICD9CM

            * `ICD9Proc` - ICD9Proc

            * `SNOMED` - SNOMED

            * `CPT4` - CPT4

            * `HCPCS` - HCPCS

            * `RxNorm` - RxNorm

            * `RxNorm Extension` - RxNorm Extension

            * `OPCS4` - OPCS4

            * `DRG` - DRG

            * `LOINC` - LOINC

            * `ICDO3` - ICDO3

            * `ATC` - ATC'
      required:
      - concept_code
      - source_vocabulary
      - target_vocabulary
    TranslateResponse:
      type: object
      properties:
        source:
          $ref: '#/components/schemas/TranslateSource'
        results:
          type: array
          items:
            $ref: '#/components/schemas/TranslateConcept'
        metadata:
          type: object
          additionalProperties: {}
      required:
      - metadata
      - results
      - source
    TranslateSource:
      type: object
      description: 'What was asked for, and what it resolved to — kept apart.


        On a curated-crosswalk hit these MAY differ. The curated map takes WHO

        ICD-10 source codes to ICD-10-CM concepts

        (`crosswalk_writer.TARGET_VOCABULARY`), so a caller who named `ICD10`

        gets back an `ICD10CM` concept, while a caller who named `ICD10CM` gets

        one in the vocabulary they asked for.


        Because it is "may" and not "always", nothing may assume a difference —

        compare the two fields. A flat payload invited callers to read the

        resolved concept as the thing they asked about, which is the error this

        split exists to prevent.'
      properties:
        requested_code:
          type: string
          description: The code the caller sent, verbatim.
        requested_vocabulary:
          type: string
          description: The vocabulary the caller said the code belongs to.
        found_via:
          type: string
          description: '''concept'' when the requested code is a native OMOP concept, ''curated_crosswalk''
            when it was resolved through the curated local mapping table (WHO ICD-10 source codes to ICD-10-CM
            concepts). On ''curated_crosswalk'', `resolved_concept` is the mapped concept, which MAY be
            in a different vocabulary from `requested_vocabulary` — compare the two rather than assuming.'
        resolved_concept:
          allOf:
          - $ref: '#/components/schemas/TranslateConcept'
          description: The OMOP concept the request resolved to, whichever route found it.
      required:
      - found_via
      - requested_code
      - requested_vocabulary
      - resolved_concept
    UserApiUsageHistory:
      type: object
      description: Response serializer for user's API usage history list item.
      properties:
        id:
          type: string
          format: uuid
        service_type:
          type: string
        endpoint:
          type: string
        status_code:
          type: integer
        input_tokens:
          type: integer
        output_tokens:
          type: integer
        cost:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,6})?$
        response_time_ms:
          type: integer
          nullable: true
        created_at:
          type: string
          format: date-time
        request_body:
          nullable: true
        response_body:
          nullable: true
        request_redacted:
          type: boolean
        response_redacted:
          type: boolean
        error_message:
          type: string
          nullable: true
      required:
      - cost
      - created_at
      - endpoint
      - error_message
      - id
      - input_tokens
      - output_tokens
      - request_body
      - request_redacted
      - response_body
      - response_redacted
      - response_time_ms
      - service_type
      - status_code
    UserApiUsageSummary:
      type: object
      description: Response serializer for user's personal API usage summary.
      properties:
        total_requests:
          type: integer
        successful_requests:
          type: integer
        failed_requests:
          type: integer
        success_rate:
          type: number
          format: double
        total_input_tokens:
          type: integer
        total_output_tokens:
          type: integer
        total_tokens:
          type: integer
        total_cost:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,6})?$
        avg_response_time_ms:
          type: number
          format: double
          nullable: true
        period_start:
          type: string
          format: date-time
        period_end:
          type: string
          format: date-time
      required:
      - avg_response_time_ms
      - failed_requests
      - period_end
      - period_start
      - success_rate
      - successful_requests
      - total_cost
      - total_input_tokens
      - total_output_tokens
      - total_requests
      - total_tokens
    VersionEnum:
      enum:
      - v35
      - v36
      - v37
      - v38
      - v39
      - v40
      - v43
      type: string
      description: '* `v35` - v35

        * `v36` - v36

        * `v37` - v37

        * `v38` - v38

        * `v39` - v39

        * `v40` - v40

        * `v43` - v43'
    VocabularyEnum:
      enum:
      - ICD10CM
      - ICD10PCS
      - CPT4
      - RxNorm
      - SNOMED
      type: string
      description: '* `ICD10CM` - ICD10CM

        * `ICD10PCS` - ICD10PCS

        * `CPT4` - CPT4

        * `RxNorm` - RxNorm

        * `SNOMED` - SNOMED'
    WorkflowRequestRequest:
      type: object
      properties:
        query:
          type: string
          minLength: 1
          description: Clinical term to resolve
        vocabulary_id:
          type: string
          minLength: 1
          description: Vocabulary to resolve against (e.g., 'ICD10CM', 'SNOMED', 'RxNorm')
        domain_id:
          type: string
          minLength: 1
          description: Filter by domain (e.g., 'Condition')
        top_k:
          type: integer
          maximum: 100
          minimum: 1
          nullable: true
          description: Override per-pass top_k from the strategy (1-100)
        include_mappings:
          type: boolean
          default: false
          description: Include cross-vocabulary mappings for each match
        target_vocabularies:
          type: array
          items:
            type: string
            minLength: 1
          description: Target vocabularies for mappings (e.g., ['SNOMED', 'ICD10CM'])
      required:
      - query
      - vocabulary_id
    WorkflowResponse:
      type: object
      properties:
        results:
          $ref: '#/components/schemas/WorkflowResults'
        metadata:
          type: object
          additionalProperties: {}
      required:
      - metadata
      - results
    WorkflowResults:
      type: object
      properties:
        steps:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowStep'
      required:
      - steps
    WorkflowStep:
      type: object
      properties:
        step:
          type: integer
        label:
          type: string
        concept_class_id:
          type: string
          nullable: true
        matches:
          type: array
          items:
            $ref: '#/components/schemas/ResolvedConcept'
        descendants:
          type: object
          additionalProperties: {}
          nullable: true
      required:
      - concept_class_id
      - label
      - matches
      - step
  securitySchemes:
    api_key_auth:
      type: apiKey
      in: header
      name: X-API-Key
      description: API key for organization authentication. Obtain from the organization settings or API
        key management endpoint.
    gateway_bearer_auth:
      type: http
      scheme: bearer
      description: Your hpx_ API key as a Bearer token, so OpenAI-compatible SDKs work unmodified. X-API-Key
        and first-party JWTs are also accepted.
    jwt_auth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: JWT access token obtained from /login/ endpoint
tags:
- name: DRG
  description: MS-DRG classification from coded input, free clinical text, or FHIR.
- name: Vocabulary
  description: OMOP concept search, coding, translation and hierarchy resolution.
- name: Gateway
  description: PHI-safe LLM access. Text is redacted before any model sees it and re-hydrated in the answer,
    and every response carries a receipt.
- name: Account
  description: Your own API usage and cost. Readable with any credential, including a key with no scopes
    — billing transparency is not a privilege to withhold. A key sees the requests made with that key;
    a dashboard session sees the requests made in the dashboard.
servers:
- url: https://api.v2.healthproximate.com
